---
const links = [
  { name: "Home", href: "/uganda-travel-service" },
  { name: "Destinations", href: "/uganda-travel-service/destinations" },
  { name: "Plan", href: "/uganda-travel-service/plan" },
];
---

<nav>
  <ul class="navbar">
    {links.map(link => (
      <li><a href={link.href}>{link.name}</a></li>
    ))}
  </ul>
</nav>